[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npx serve dist -s -l 3000"
healthcheckPath = "/"
healthcheckTimeout = 100

[service]
internalPort = 3000
